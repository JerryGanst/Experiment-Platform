# Experiment Platform - GPU版本完整依赖
# 支持PyTorch GPU加速和深度学习模型推理

# ========== 核心深度学习框架 ==========
# PyTorch GPU版本 (CUDA 11.8+)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
--index-url https://download.pytorch.org/whl/cu118

# Transformers和相关库
transformers==4.43.3
tokenizers>=0.13.0
accelerate>=0.20.0

# ========== 数据处理和科学计算 ==========
numpy>=1.20.0
pandas>=1.3.0
datasets>=2.10.0

# ========== GPU监控和性能工具 ==========
pynvml>=11.0.0
nvidia-ml-py3>=7.352.0
psutil>=5.8.0

# ========== 文本处理和评估 ==========
rouge>=1.0.1
rouge_score>=0.1.2
fuzzywuzzy>=0.18.0
jieba==0.42.1

# ========== 进度条和UI ==========
tqdm==4.66.1
matplotlib>=3.5.0
seaborn>=0.12.0

# ========== 模板和配置 ==========
jinja2==3.1.0
pyyaml>=6.0
toml>=0.10.2

# ========== 特殊工具 ==========
icetk==0.0.7
packaging>=21.0

# ========== 可选：Flash Attention (如果需要) ==========
# flash-attn>=2.0.0  # 需要CUDA编译环境，可选安装

# ========== VLLM推理引擎（可选） ==========
# VLLM高性能推理后端 - 需要CUDA 11.8+ 和 Linux系统
# 安装命令: pip install vllm
# vllm>=0.4.0  # VLLM核心引擎，PagedAttention支持
# xformers>=0.0.23  # 内存高效注意力实现
# ray>=2.9.0  # 分布式推理支持（可选）
# 注意：VLLM与特定PyTorch/CUDA版本有严格兼容性要求
# 详见：https://docs.vllm.ai/en/latest/getting_started/installation.html

# ========== HTTP客户端（VLLM Server模式需要） ==========
httpx>=0.24.0  # 异步HTTP客户端
aiohttp>=3.8.0  # 异步HTTP请求

# ========== 开发和测试工具 ==========
pytest>=7.0.0
pytest-asyncio>=0.21.0  # 异步测试支持
black>=22.0.0
flake8>=4.0.0